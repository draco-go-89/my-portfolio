# Mobile Responsiveness Fix Plan

## ✅ Step 1: Fix `videos.html` hamburger menu
- Removed "FU"/"CK" text from hamburger spans
- Fixed span structure to match `index.html` (3 empty spans)

## ✅ Step 2: Add `back-to-top` button to `videos.html`
- Added back-to-top button (same as `index.html`)

## ✅ Step 3: Fix mobile responsive issues in CSS
- Added `scroll-margin-top: clamp(70px, 8vh, 90px)` to all sections so fixed navbar doesn't cover section content on anchor scroll

## ✅ Step 4: Add theme-toggle to `index.html` navbar
- Added theme-toggle button for consistency with `videos.html`

---

**All changes completed!**

